name: ego-autopilot
on:
  workflow_dispatch:
  schedule:
    - cron: "17 3 1 * *"
permissions:
  contents: read
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: hello
        run: echo "ego-autopilot DISPATCH OK"